include required("conveyor.conf")

app {
  signing-key = ${env.SIGNING_KEY}

  windows {
    certificate = wincert.p12
    signing-key = wincert.p12
  }

//  mac {
//    certificate = maccert.p12
//    signing-key = maccert.p12
//
//    notarization {
//      app-specific-password = ${env.APPLE_ASP}
//      team-id = TODO
//      apple-id = "TODO"
//    }
//  }
  mac = null

  site {
    github {
      pages-branch = "gh-pages"
      oauth-token = ${env.GITHUB_TOKEN}
    }
  }
}
